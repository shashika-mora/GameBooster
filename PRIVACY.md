# Privacy

GameBooster stores game paths, profile settings, session records, and recovery state in a local SQLite database. Monitoring samples stay in memory. No analytics, accounts, cloud sync, or network calls are implemented. Removing the application does not automatically remove user data from the Windows application data directory.
